import{_ as a,o as n,c as i,ap as t}from"./chunks/framework.Cr9LhhWL.js";const h=JSON.parse('{"title":"Developer guide","description":"","frontmatter":{},"headers":[],"relativePath":"man/developer.md","filePath":"man/developer.md","lastUpdated":null}'),s={name:"man/developer.md"};function o(d,e,r,l,c,p){return n(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="Developer-guide" tabindex="-1">Developer guide <a class="header-anchor" href="#Developer-guide" aria-label="Permalink to &quot;Developer guide {#Developer-guide}&quot;">​</a></h1><p>This page maps the implementation so contributors can find the right layer before changing code. For local setup, tests, formatting, and pull-request requirements, see <a href="/JustRelax.jl/previews/PR527/man/contributing#Contributing">Contributing</a>.</p><h2 id="Architecture-at-a-glance" tabindex="-1">Architecture at a glance <a class="header-anchor" href="#Architecture-at-a-glance" aria-label="Permalink to &quot;Architecture at a glance {#Architecture-at-a-glance}&quot;">​</a></h2><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>JustRelax</span></span>
<span class="line"><span>├── src/JustRelax.jl        public types, backend tags, top-level exports</span></span>
<span class="line"><span>├── src/JustRelax_CPU.jl    CPU 2D and 3D modules</span></span>
<span class="line"><span>├── src/common.jl           code included by every backend/dimension module</span></span>
<span class="line"><span>├── src/stokes/             standard pseudo-transient Stokes solvers</span></span>
<span class="line"><span>├── src/variational_stokes/ variational and free-surface Stokes solvers</span></span>
<span class="line"><span>├── src/DYREL/              dynamic-relaxation solver</span></span>
<span class="line"><span>├── src/thermal_diffusion/  pseudo-transient and explicit thermal solvers</span></span>
<span class="line"><span>├── src/rheology/           material-property evaluation and derived fields</span></span>
<span class="line"><span>├── src/boundaryconditions/ boundary-condition types and kernels</span></span>
<span class="line"><span>├── src/grid/, src/types/   grids, state, traits, and constructors</span></span>
<span class="line"><span>└── ext/                    CUDA, AMDGPU, and Makie extensions</span></span></code></pre></div><p><code>JustRelax2D</code> and <code>JustRelax3D</code> are the modules users import after <code>using JustRelax</code>. They include <code>common.jl</code>, then their dimension-specific solver entry points. The shared file provides constructors, grid utilities, material updates, boundary conditions, interpolation, and compute kernels.</p><h2 id="Backends-and-dimensions" tabindex="-1">Backends and dimensions <a class="header-anchor" href="#Backends-and-dimensions" aria-label="Permalink to &quot;Backends and dimensions {#Backends-and-dimensions}&quot;">​</a></h2><p>JustRelax writes solver logic once using ParallelStencil and dispatches on a backend trait. The CPU modules initialize ParallelStencil with <code>Threads</code>; the CUDA and AMDGPU extensions re-include the same shared source after initializing their own 2D or 3D module.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>array/state → backend(state) → CPU/CUDA/AMDGPU trait → shared implementation</span></span></code></pre></div><p>The extensions own device-array allocation (<code>PTArray</code>) and trait dispatch. Do not branch on <code>isa CuArray</code> or allocate plain <code>Array</code>s inside backend-generic code. Read <a href="/JustRelax.jl/previews/PR527/man/backend#Selecting-the-backend">Selecting the backend</a> for the user-facing setup, including the separate JustRelax and JustPIC backend tags.</p><p><code>@init_parallel_stencil</code> can run only once per module in a Julia session. A change of backend or dimensionality therefore requires a fresh Julia process. Never attempt to reinitialize ParallelStencil in an already-loaded solver module.</p><h2 id="Adding-a-solver" tabindex="-1">Adding a solver <a class="header-anchor" href="#Adding-a-solver" aria-label="Permalink to &quot;Adding a solver {#Adding-a-solver}&quot;">​</a></h2><ol><li><p>Start from the closest existing family: standard Stokes in <code>src/stokes/</code>, variational/free-surface Stokes in <code>src/variational_stokes/</code>, DYREL in <code>src/DYREL/</code>, or thermal diffusion in <code>src/thermal_diffusion/</code>.</p></li><li><p>Keep 2D and 3D entry points separate. A 2D source file must not be included from a 3D module, or the reverse.</p></li><li><p>Put code that is genuinely independent of both dimension and device in <code>src/common.jl</code> or one of the files it includes. Reuse <code>src/MiniKernels.jl</code> for existing staggered-grid helpers.</p></li><li><p>Dispatch through the backend trait, following the public entry point → CPU trait → shared implementation pattern used by <code>solve!</code>. The extensions must include every source file required by the new solver.</p></li><li><p>Keep kernels GPU-safe: no allocations, dynamic dispatch, exceptions, or printing inside a kernel. Use <code>@parallel</code> for stencil kernels and <code>@parallel_indices</code> when explicit indices or cell-array access are needed.</p></li><li><p>Add focused CPU tests first. Run accelerator tests only on the corresponding hardware and report the backends actually checked.</p></li></ol><h2 id="Adding-material-behavior" tabindex="-1">Adding material behavior <a class="header-anchor" href="#Adding-material-behavior" aria-label="Permalink to &quot;Adding material behavior {#Adding-material-behavior}&quot;">​</a></h2><p>Most model rheology does <strong>not</strong> require a JustRelax change. Define phases and their material properties with GeoParams&#39; <code>SetMaterialParams</code> in the miniapp, then pass the resulting <code>rheology</code> and, when needed, JustPIC <code>PhaseRatios</code> to <code>compute_viscosity!</code>, <code>compute_ρg!</code>, and the solver.</p><p>Add code under <code>src/rheology/</code> only when JustRelax needs a new derived field or solver-side evaluation. Follow the existing split:</p><ul><li><p><code>Viscosity.jl</code> evaluates effective viscosity from the selected invariant and phase ratios.</p></li><li><p><code>BuoyancyForces.jl</code> evaluates density times gravity.</p></li><li><p><code>StressUpdate.jl</code>, <code>Melting.jl</code>, and <code>Solubility.jl</code> implement specialized updates.</p></li></ul><p>Include a new file from <code>src/common.jl</code>, accept generic array and rheology arguments unless the implementation requires more, and ensure the kernel works for CPU, CUDA, and AMDGPU arrays. Add the smallest test that exercises the new material path.</p><h2 id="Adding-a-boundary-condition" tabindex="-1">Adding a boundary condition <a class="header-anchor" href="#Adding-a-boundary-condition" aria-label="Permalink to &quot;Adding a boundary condition {#Adding-a-boundary-condition}&quot;">​</a></h2><p>First check whether the existing constructors cover the model:</p><ul><li><p><code>TemperatureBoundaryConditions</code> supports no-flux, constant-flux, constant-value, periodic, and mask-based Dirichlet conditions.</p></li><li><p><code>VelocityBoundaryConditions</code> and <code>DisplacementBoundaryConditions</code> support no-slip, free-slip, periodic, and free-surface conditions.</p></li></ul><p>For a new condition, add its type or kernel under <code>src/boundaryconditions/</code> and include it through <code>BoundaryConditions.jl</code>, which is already shared by CPU and GPU modules. Validate incompatible face combinations when constructing the condition. Apply thermal conditions through <code>thermal_bcs!</code> and flow conditions through <code>flow_bcs!</code>; preserve the trait-dispatch pattern so device-specific modules can reuse the same implementation.</p><h2 id="Validation-checklist" tabindex="-1">Validation checklist <a class="header-anchor" href="#Validation-checklist" aria-label="Permalink to &quot;Validation checklist {#Validation-checklist}&quot;">​</a></h2><p>Before opening a pull request:</p><ol><li><p>Run the smallest affected CPU test.</p></li><li><p>Format changed Julia files with Runic.</p></li><li><p>Build the documentation if public behavior or docs changed.</p></li><li><p>Review <code>git diff --check</code> and <code>git status</code> for unrelated files.</p></li></ol><p>The <a href="/JustRelax.jl/previews/PR527/man/contributing#Contributing">Contributing</a> page has the exact commands and backend-specific test invocations.</p>`,25)])])}const m=a(s,[["render",o]]);export{h as __pageData,m as default};
